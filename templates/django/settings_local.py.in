"""
This is an AUTOMATICALLY GENERATED file.
!!! DO NOT EDIT MANUALLY !!!
"""
import os
import sys


ROOT = os.path.dirname(os.path.abspath(__file__))

DEBUG = ${project-env:django-debug}
TEMPLATE_DEBUG = DEBUG

ENVIRONMENT = '${project-env:env-name}'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2', # Add 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': '${project-env:db-name}',                    # Or path to database file if using sqlite3.
        'USER': '${project-env:db-user}',                    # Not used with sqlite3.
        'PASSWORD': '${project-env:db-pass}',                # Not used with sqlite3.
        'HOST': '${project-env:db-host}',                    # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '${project-env:db-port}',                    # Set to empty string for default. Not used with sqlite3.
    }
}

# Make this unique, and don't share it with anybody.
SECRET_KEY = '${project-env:django-secret}'

# Absolute path to the directory static files should be collected to.
# Don't put anything in this directory yourself; store your static files
# in apps' "static/" subdirectories and in STATICFILES_DIRS.
# Example: "/home/media/media.lawrence.com/static/"
STATIC_ROOT = '${buildout:directory}/var/static/'

# Absolute filesystem path to the directory that will hold user-uploaded files.
# Example: "/home/media/media.lawrence.com/media/"
MEDIA_ROOT = '${buildout:directory}/media/'

MEDIA_URL = '/media/'

# Additional locations of static files
STATICFILES_DIRS = (
    # Put strings here, like "/home/html/static" or "C:/www/django/static".
    # Always use forward slashes, even on Windows.
    # Don't forget to use absolute paths, not relative paths.
    '${buildout:directory}/djangoroot/project/assets',
)

TEMPLATE_DIRS = (
    # Put strings here, like "/home/html/django_templates" or "C:/www/django/templates".
    # Always use forward slashes, even on Windows.
    # Don't forget to use absolute paths, not relative paths.
    '${buildout:directory}/djangoroot/project/templates',
)

TEMPLATE_CONTEXT_PROCESSORS = (
    "django.contrib.auth.context_processors.auth",
    "django.core.context_processors.debug",
    "django.core.context_processors.i18n",
    "django.core.context_processors.media",
    "django.core.context_processors.static",
    "django.core.context_processors.tz",
    "django.core.context_processors.request",
    "django.contrib.messages.context_processors.messages",
    "project.processor.view_id",
    "project.processor.categories",
    "project.processor.coin_stats",
    #"payments.context_processors.payments_settings",
)

INSTALLED_APPS = (
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.sites',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Uncomment the next line to enable the admin:
    'django.contrib.admin',
    # Uncomment the next line to enable admin documentation:
    # 'django.contrib.admindocs',
    'gunicorn',
    'south',
    'django_extensions',
    'project',
    'rest_framework',
    'github_hook',
    'polymorphic', # We need polymorphic installed for the shop
    'shop', # The django SHOP application
    'shop.addressmodel', # The default Address and country models
    'coin',
    'registration',
    'contact_form',
    'debug_toolbar',
    'simplejson',
    'payments'
)

GOOGLECHARTS_PACKAGES = ["corechart"]

LOGIN_REDIRECT_URL = '/'

ACCOUNT_ACTIVATION_DAYS = 7

EMAIL_BACKEND = 'django.core.mail.backends.filebased.EmailBackend'
EMAIL_FILE_PATH = os.path.join(ROOT, "tmp", "email")

SHOP_FORCE_LOGIN = True
SHOP_SHIPPING_BACKENDS = (
    'project.shipping.SkipShippingBackend',
)

SHOP_PAYMENT_BACKENDS = (
    'project.payment.PayWithCoinsBackend',
)

SHOP_OWNER_USERNAME = "jeremy"

STRIPE_SECRET_KEY = os.environ.get(
    "STRIPE_SECRET_KEY",
    "${project-env:stripe-key-private}"
)
STRIPE_PUBLIC_KEY = os.environ.get(
    "STRIPE_PUBLIC_KEY",
    "${project-env:stripe-key-public}"
)
